import React, { FC } from "react";

const Divider: FC = () => {
  return (
    <div
      style={{
        height: "1px",
        width: "100%",
        backgroundColor: "#262626",
        marginTop: "5px",
        marginBottom: "5px",
      }}
    />
  );
};

export default Divider;
